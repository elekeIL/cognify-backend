# Nixpacks configuration for Railway deployment

[phases.setup]
nixPkgs = ["file", "ffmpeg"]  # 'file' provides libmagic, ffmpeg for audio

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
