# Nixpacks configuration for Railway deployment

[phases.setup]
nixPkgs = ["file", "ffmpeg"]
aptPkgs = ["libmagic1", "ffmpeg"]

[phases.build]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
